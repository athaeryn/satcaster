#! /usr/bin/env python3

from vec3 import vec3

from lib.camera import Camera
from lib.sphere import Sphere

if __name__ == "__main__":
    print("Later, World...")
    cam = Camera(vec3(0, 1, 0))
    cam.log()

    s = Sphere()
    s.log()
